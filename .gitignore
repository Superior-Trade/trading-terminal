# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env (keep the example tracked)
.env*
!.env.example

# embedded database
.data/

# TradingView Advanced Charts — licensed to you directly by TradingView and
# not redistributable, so it is never committed. See docs/charting-library.md.
public/static/charting_library/
public/static/datafeeds/

# editors / OS
.DS_Store
.idea/
.vscode/

# staged for upload to GitHub's CDN, never committed (see docs/media/README.md)
docs/media/demo.mp4

# local markdown preview output
.preview.html
